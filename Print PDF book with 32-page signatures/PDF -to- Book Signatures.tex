\documentclass{article}
\usepackage{pdfpages}
\begin{document}

% includepdf arguments:
%  In square brackets:
%    signature = Number of pages per signature (or per block)
%    pages = 
%    'landscape' (or 'portrait') for layout on sheet
%    delta=x y for page offset (determines final page margins)
%  {path/to/filename.pdf}

% Update the SIGNATURE argument as needed!
% 32 page signature = 8 double-sided sheets per signature, good for stitch-bound blocks
% 4 page signature = 1 double-sided sheet per signature, good for quick glue-binding

\includepdf[signature=4,pages=-,landscape,delta=0 50]{/Users/Administrator/Desktop/filename.pdf}


\end{document}
